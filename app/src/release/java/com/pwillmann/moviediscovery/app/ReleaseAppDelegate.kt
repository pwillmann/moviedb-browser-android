package com.pwillmann.moviediscovery.app

import android.app.Application

class ReleaseAppDelegate : AppDelegate() {

    // Used in debug builds to init debug libs
    override fun onCreate(application: Application) {
    }
}
